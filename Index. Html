<!DOCTYPE html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>האנימציה המורכבת ביותר ב-GitHub Pages</title>
    <style>
        body, html { margin: 0; padding: 0; width: 100%; height: 100%; overflow: hidden; background-color: #000; }
        canvas { width: 100%; height: 100%; }
        .ui { position: absolute; top: 20px; left: 20px; color: white; font-family: sans-serif; pointer-events: none; text-shadow: 0 2px 4px rgba(0,0,0,0.5); }
    </style>
</head>
<body>
    <div class="ui">
        <h1>Fluid Simulation WebGL</h1>
        <p>הזיזו את העכבר או געו במסך כדי להפעיל את הפיזיקה</p>
    </div>
    <canvas id="canvas"></canvas>

    <!-- טעינת הסימולציה ישירות מ-CDN כדי לשמור על קובץ אחד פשוט -->
    <script src="https://unpkg.com/webgl-fluid-enhanced@1.0.3/dist/webgl-fluid-enhanced.umd.js"></script>
    <script>
        const canvas = document.getElementById('canvas');
        webGLFluidEnhanced.config({
            SIM_RESOLUTION: 128,
            DYE_RESOLUTION: 1024,
            DENSITY_DISSIPATION: 0.97,
            VELOCITY_DISSIPATION: 0.98,
            PRESSURE: 0.8,
            CURL: 30,
            SPLAT_AMOUNT: 5,
            INITIAL: true,
            COLOR_PALETTE: ['#33fffc', '#ff33fb', '#ff8133', '#33ff5a'],
            BLOOM: true,
            SUNRAYS: true
        });
        webGLFluidEnhanced.start(canvas);
    </script>
</body>
</html>
